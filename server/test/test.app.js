var assert = require("assert"), 
    should = require("should"),
    draft = require('../app.js');
 
describe('io.sockets', function () { 
    describe('#createPlayer()', function(){

    });
});
